# /etc/default/leil-chunkserver

# Start leil-chunkserver from init.d script on boot.
#   Only allowed values are "true" and "false".
#   Undefined values default to "false".
SAUNAFSCHUNKSERVER_ENABLE=false

DAEMON_OPTS=""
