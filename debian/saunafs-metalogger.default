# /etc/default/leil-metalogger

# Start leil-metalogger from init.d script on boot.
#   Only allowed values are "true" and "false".
#   Undefined values default to "false".
SAUNAFSMETALOGGER_ENABLE=false

DAEMON_OPTS=""
